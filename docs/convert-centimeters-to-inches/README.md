---
package:
    - npm
title: Convert Centimeters to Inches
description: Convert a length in centimeters to inches
slug: convert-centimeters-to-inches
function: ConvertCentimetersToInches
category:
    - Math
    - Length
    - Centimeters
    - Inches
    - Centimeters to Inches
    - Length Conversion
---

<CodeBlock>

```js
import { ConvertCentimetersToInches } from "open-tools";

let centimeters = 0;

let result = ConvertCentimetersToInches(centimeters);

console.log(`${centimeters}cm = ${result}"`);
```

```ts
import { ConvertCentimetersToInches } from "open-tools";

let centimeters: number = 0;

let result: number = ConvertCentimetersToInches(centimeters);

console.log(`${centimeters}cm = ${result}"`);
```

</CodeBlock>
